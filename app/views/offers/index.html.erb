<h1>Available Motorcicles</h1>
<ul>
  <% @offers.each do |offer| %>
    <li><%= offer.description %> <%= link_to 'Details', offer_path(offer) %></li>
  <% end %>
</ul>
<%= link_to 'Back', root_path %>
